# 🏗️ BLOC ARCHITECTURE VISUALIZATION

```
┌─────────────────────────────────────────────────────────────────┐
│                           YOUR APP                              │
└─────────────────────────────────────────────────────────────────┘
                                │
                                │
                ┌───────────────▼───────────────┐
                │         main.dart             │
                │  ┌─────────────────────────┐  │
                │  │  MultiRepositoryProvider│  │
                │  │  - AuthRepository       │  │
                │  │  - CashierRepository    │  │
                │  └────────────┬────────────┘  │
                │               │               │
                │  ┌────────────▼────────────┐  │
                │  │   MultiBlocProvider     │  │
                │  │   - AuthCubit           │  │
                │  │   - CashierCubit        │  │
                │  └────────────┬────────────┘  │
                └───────────────┼───────────────┘
                                │
        ┌───────────────────────┴───────────────────────┐
        │                                               │
        ▼                                               ▼
┌───────────────┐                              ┌───────────────┐
│  Login Screen │                              │Cashier Screen │
│  ┌──────────┐ │                              │  ┌──────────┐ │
│  │BlocConsumer│                              │  │BlocConsumer│
│  │ AuthCubit│ │                              │  │CashierCubit│
│  └────┬─────┘ │                              │  └────┬─────┘ │
│       │       │                              │       │       │
│   ┌───▼────┐  │                              │   ┌───▼────┐  │
│   │ Login  │  │                              │   │ Header │  │
│   │ Button │──┼──┐                           │   │ Section│  │
│   └────────┘  │  │                           │   └────────┘  │
│               │  │                           │   ┌────────┐  │
│               │  │                           │   │Services│  │
└───────────────┘  │                           │   │ Grid   │  │
                   │                           │   └────────┘  │
                   │                           │   ┌────────┐  │
                   │                           │   │  Cart  │  │
                   │                           │   │ Section│  │
                   │                           │   └────────┘  │
                   │                           └───────────────┘
                   │
                   │
        ┌──────────▼──────────┐
        │     AuthCubit       │
        │  ┌───────────────┐  │
        │  │ login()       │  │
        │  │ logout()      │  │
        │  │ checkAuth()   │  │
        │  └───────┬───────┘  │
        │          │          │
        │  ┌───────▼───────┐  │
        │  │   States:     │  │
        │  │ - Loading     │  │
        │  │ - Authenticated│ │
        │  │ - Error       │  │
        │  └───────┬───────┘  │
        └──────────┼──────────┘
                   │
        ┌──────────▼──────────┐
        │   AuthRepository    │
        │  ┌───────────────┐  │
        │  │ login()       │  │
        │  │ logout()      │  │
        │  │ isLoggedIn()  │  │
        │  └───────┬───────┘  │
        │          │          │
        │  ┌───────▼───────┐  │
        │  │  Mock/Real    │  │
        │  │  Backend API  │  │
        │  └───────────────┘  │
        └─────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                     DATA FLOW DIAGRAM                           │
└─────────────────────────────────────────────────────────────────┘

User Action (Button Click)
        │
        ▼
┌─────────────────────┐
│  UI Widget          │
│  (Button/TextField) │
└──────────┬──────────┘
           │
           │ context.read<CashierCubit>().addToCart(...)
           │
           ▼
┌─────────────────────┐
│  CashierCubit       │◄─── Business Logic Layer
│  - addToCart()      │
│  - removeFromCart() │
└──────────┬──────────┘
           │
           │ emit(CashierLoading)
           │
           ▼
┌─────────────────────┐
│  CashierRepository  │◄─── Data Layer
│  - saveCart()       │
│  - fetchServices()  │
└──────────┬──────────┘
           │
           │ API Call / Local Storage
           │
           ▼
┌─────────────────────┐
│  Backend / Storage  │◄─── External Systems
└──────────┬──────────┘
           │
           │ Response
           ▼
┌─────────────────────┐
│  CashierCubit       │
│  emit(CashierLoaded)│
└──────────┬──────────┘
           │
           │ State Change
           │
           ▼
┌─────────────────────┐
│  BlocBuilder        │◄─── Listens to State
│  Rebuilds Widget    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Updated UI         │
│  Displays new data  │
└─────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    LAYER RESPONSIBILITIES                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────┐
│  UI LAYER (Screens & Widgets)       │
│  ═════════════════════════════════  │
│  ✓ Display data                     │
│  ✓ Handle user input                │
│  ✓ Call cubit methods               │
│  ✓ Listen to state changes          │
│  ✗ NO business logic                │
│  ✗ NO data fetching                 │
└─────────────────────────────────────┘
              │
              │ Uses
              ▼
┌─────────────────────────────────────┐
│  CUBIT LAYER (Business Logic)       │
│  ═════════════════════════════════  │
│  ✓ Manage state                     │
│  ✓ Business logic                   │
│  ✓ Coordinate data flow             │
│  ✓ Emit states                      │
│  ✗ NO UI code                       │
│  ✗ NO direct API calls              │
└─────────────────────────────────────┘
              │
              │ Uses
              ▼
┌─────────────────────────────────────┐
│  REPOSITORY LAYER (Data)            │
│  ═════════════════════════════════  │
│  ✓ Fetch data from APIs             │
│  ✓ Local storage operations         │
│  ✓ Data transformation              │
│  ✗ NO state management              │
│  ✗ NO UI logic                      │
└─────────────────────────────────────┘
              │
              │ Calls
              ▼
┌─────────────────────────────────────┐
│  EXTERNAL (APIs, DB, Storage)       │
│  ═════════════════════════════════  │
│  • Backend REST API                 │
│  • Local Database                   │
│  • Shared Preferences               │
│  • File System                      │
└─────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│              STATE LIFECYCLE IN YOUR APP                        │
└─────────────────────────────────────────────────────────────────┘

APP START
   │
   ├─► AuthCubit.checkAuthStatus()
   │      │
   │      ├─► emit(AuthChecking)  ──► Show Splash Screen
   │      │
   │      └─► emit(AuthUnauthenticated) ──► Show Login Screen
   │
   │
USER LOGS IN
   │
   ├─► AuthCubit.login(username, password)
   │      │
   │      ├─► emit(AuthLoading) ──► Show Loading
   │      │
   │      ├─► AuthRepository.login() ──► Call API
   │      │
   │      ├─► emit(AuthAuthenticated) ──► Navigate to Cashier
   │      │
   │      └─► emit(AuthError) ──► Show Error Toast
   │
   │
CASHIER SCREEN OPENS
   │
   ├─► CashierCubit.initialize()
   │      │
   │      ├─► emit(CashierLoading) ──► Show Loading
   │      │
   │      ├─► Repository.fetchServices()
   │      ├─► Repository.fetchCustomers()
   │      ├─► Repository.fetchBarbers()
   │      │
   │      └─► emit(CashierLoaded) ──► Show Data
   │
   │
USER ADDS ITEM
   │
   ├─► CashierCubit.addToCart(service, barber)
   │      │
   │      ├─► Update cart list
   │      │
   │      ├─► emit(CashierLoaded) ──► Update UI
   │      │
   │      ├─► emit(CashierItemAdded) ──► Show Toast
   │      │
   │      └─► emit(CashierLoaded) ──► Return to normal
   │
   │
USER SUBMITS INVOICE
   │
   └─► CashierCubit.submitInvoice()
          │
          ├─► emit(CashierSubmittingInvoice) ──► Show Loading
          │
          ├─► Repository.submitInvoice() ──► Send to API
          │
          ├─► emit(CashierInvoiceSubmitted) ──► Show Success
          │
          └─► emit(CashierLoaded with empty cart) ──► Reset


┌─────────────────────────────────────────────────────────────────┐
│                  WIDGET TREE WITH BLOC                          │
└─────────────────────────────────────────────────────────────────┘

MaterialApp
 └─ MultiRepositoryProvider ←─── Provides Repositories
     ├─ AuthRepository
     └─ CashierRepository
      │
      └─ MultiBlocProvider ←─── Provides Cubits
          ├─ BlocProvider<AuthCubit>
          └─ BlocProvider<CashierCubit>
           │
           └─ BlocBuilder<AuthCubit> ←─── Decides which screen
               │
               ├─ LoginScreen (if not authenticated)
               │   └─ BlocConsumer<AuthCubit> ←─── Handles login
               │       ├─ listener: Show errors
               │       └─ builder: Show loading
               │
               └─ CashierScreen (if authenticated)
                   └─ BlocConsumer<CashierCubit> ←─── Handles data
                       ├─ listener: Show toasts
                       └─ builder: Show UI
                           │
                           ├─ HeaderSection
                           │   └─ BlocBuilder<CashierCubit>
                           │       └─ Get customers list
                           │
                           ├─ CategoriesSection
                           │   └─ Calls selectCategory()
                           │
                           ├─ ServicesGrid
                           │   └─ BlocBuilder<CashierCubit>
                           │       └─ Get filtered services
                           │
                           └─ CartSection
                               └─ BlocBuilder<CashierCubit>
                                   └─ Get cart items


┌─────────────────────────────────────────────────────────────────┐
│                   FILES YOU CREATED                             │
└─────────────────────────────────────────────────────────────────┘

repositories/
├─ auth_repository.dart         ← Handles login/logout API calls
└─ cashier_repository.dart      ← Handles services/cart/customers

cubits/
├─ auth/
│  ├─ auth_cubit.dart           ← Login/logout logic
│  └─ auth_state.dart           ← Auth states
└─ cashier/
   ├─ cashier_cubit.dart        ← Cart/services logic
   └─ cashier_state.dart        ← Cashier states


┌─────────────────────────────────────────────────────────────────┐
│                  FILES YOU NEED TO UPDATE                       │
└─────────────────────────────────────────────────────────────────┘

lib/
├─ main.dart                    ← Add providers
├─ screens/
│  ├─ auth/
│  │  └─ login_screen.dart     ← Use AuthCubit
│  └─ casher/
│     ├─ casher_screen.dart    ← Use CashierCubit
│     └─ header_section.dart   ← Use CashierCubit for customers


That's it! You now have a complete separation of concerns:
• UI only displays and captures input
• Cubit handles all business logic
• Repository handles all data operations

Happy coding! 🚀
